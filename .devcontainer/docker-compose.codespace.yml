version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: devpassword123
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U keycloak"]
      interval: 5s
      timeout: 3s
      retries: 5

  keycloak:
    image: quay.io/keycloak/keycloak:24.0
    command: start-dev --import-realm
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: devpassword123
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: devpassword123
      # Allow any hostname so Codespace dynamic URLs work
      KC_HOSTNAME_STRICT: "false"
      KC_HTTP_ENABLED: "true"
    expose:
      - "8080"
    volumes:
      - ../k8s/keycloak-realm-config.dev.json:/opt/keycloak/data/import/realm.json

  app:
    image: node:20-alpine
    # 安裝 bash 讓 VS Code 終端機可正常使用
    command: sh -c "apk add --no-cache bash && sleep infinity"
    working_dir: /workspace
    volumes:
      - ..:/workspace:cached
      - node_modules:/workspace/node_modules
      - /var/run/docker.sock:/var/run/docker.sock
    expose:
      - "3000"
    depends_on:
      - keycloak

volumes:
  pg_data:
  node_modules:
